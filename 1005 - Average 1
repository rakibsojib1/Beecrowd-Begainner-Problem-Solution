#include <iostream>
#include <iomanip>
using namespace std;
 
int main() {

    double a,b,c,avg;
    cin>>a>>b;

    c = (a*3.5)+(b*7.5);
    avg = c / (3.5+7.5);

    cout<<"MEDIA = "<<fixed<< setprecision(5) <<avg<<endl;
 
    return 0;
}
